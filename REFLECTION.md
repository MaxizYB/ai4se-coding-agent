# REFLECTION.md

> 本文件由学生本人撰写（§六：反思报告禁止 AI 代写）。AI 仅用于格式整理与排版，未修改任何正文内容。

---

## 反思

### 1. 哪些 Superpowers 技能发挥了最大作用？哪些"形式大于实质"？

我认为 Brainstorming 环节可能是发挥了最大的作用，在这个过程中，逐渐把项目的整体架构思路建立起来，有利于工程师 check 中间过程。例如，这个过程能够帮我发现一些模型错误的方向，比如它在第一次执行的过程中，将项目需求理解的偏窄，而且对于项目中所说的 TDD，他认为是我们要开发一个TDD，而不是用TDD来进行开发。再例如，我是在多次提醒后，LLM才意识到应该做一个 coding agent，而非单纯的 test agent；关于"形式大于实质"，我认为应该关注如何定义这个问题，我们可以将其理解为走了形式但是没有产生真正的价值，在本项目中有一个很具体的例子，就是尽管每次test都是全绿，但是最后LLM真实环境下一跑出现了5个bug，这倒不是说test不应该存在，而是应该思考如何构造test，现在这种让AI

### 2. TDD 强制在 AI 协作下是阻碍还是放大器？

我认为目前 TDD 有作用，但是用处不是很大，原因是 Test 难度太低，LLM构造的不够好，导致每次Test都通过，但是实际上线还是有很多问题，关键是LLM对于需求不清楚，而且它并没有使用生产过程中的真实环境测试，构造的大多数 mock 都是它自己构造出来的。

### 3. subagent-driven 工作流让智能体能自主运行多久而不偏离主题？

我认为这个和 模型 以及 harness 有关，在我测试的版本：GLM 5.2 + opencode，我认为能够持续运行很长时间，主 agent 能够保持较长时间的低 ctx，脏活都交给 subagent 干，这样能够提升长期稳定性，但是问题就是 token 使用量上升。此外，虽然没有偏离主线，但是因为模型能力限制，他没有很好理解文档需求，导致一开始制定计划的时候就已经有一些偏离了。

### 4. 什么样的 task 颗粒度最优？

我认为适中的最好，一方面不能太大，尤其是不能是"从零直接构建一个agent"；另外太小了则体现不出 agent 能力

### 5. SPEC / PLAN 质量如何影响实现质量？

SPEC 描述不清楚的确会导致模型从一开始设计就偏离，正如我上面所说的，因为 SPEC 不清楚，模型一开始就理解错了，理解成只做一个测试智能体

### 6. 你最有效的 prompt / context 策略是什么？为什么有效？

有效的 prompt 应该明确具体范围，指出具体问题，缩小指令范围，而不是模糊的、宽泛的需求。

### 7. 凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？

凭据不能硬编码或者混杂在代码中，否则很容易泄露，同时要确保分发不能包含凭据，所以凭据只能走环境变量这种形式。分发方面，我们选定的是 docker，确保能够正常一键运行。

### 8. 如果重做你会改变什么？

我想我会从一开始再把SPEC 和 过程文档再手动重写一下，并且要求模型迭代开发，先从骨架开发，把架构搭好，而不是专注某一个具体功能

### 9. 对 Superpowers 这套方法论的批判——它假设了什么？这些假设在你的项目里成立吗？

该问题可以具体分析：

- brainstorming：部分不成立，因为一开始LLM对需求理解窄了，还是有问题的，而且这个阶段很多都是夹杂了模型的主观意见，实际上程序员大部分时间都是按照模型推荐的走，那么这一过程的意义也被大幅削弱
- TDD：最需要修改，必须引入强有力的Test，否则只是摆设
- cold-start: 比较成立，模型发现了11处问题
- 分task+subagent：非常有效，保持主模型对话窗口不会爆炸，确保了较强的一致性
- plan：这个也是因为模型自身的问题，多处代码和自身测试矛盾

### 10. mock 假绿——项目中反复出现的核心教训

- 冷启动(Phase 2):手工 fixture 没包 `<testsuites>` 外壳 → parser 在真实 pytest 上读不到失败 → 全绿假象。
- 集成阶段(Phase 5):mock 脚本永远 emit 完美协议(有 type= 属性) → 真实 pytest `<failure>` 没 type= → 绿色测试被分类成 UNKNOWN → 深做维度在真实使用中静默失效。
- live 冒烟(Phase 8):mock 不需要 PATH、不关心 junit 相对路径、不会 emit 裸 list_dir → 真实 LLM 一跑 5 个 bug,测试全绿一个没抓到。
- 沙箱漏洞(Phase 9):@mention 路径遍历读仓库外文件 → 沙箱读边界破了。mock 测试的 @path 都是合法路径 → 没测 @../../etc/passwd。

### 11. 治理深做的意外发现

但评审发现:

- Sandbox 用 abspath 不防 symlink → src/evil -> /etc 绕过写范围。
- run_shell 完全不受 write_roots 约束(sed -i/> 随便写)。
- Containerize 无 executor 时 fail-open(应该 fail-closed)。
- 批处理默认 diff_preview="ask" + FailClosedApprover → 拒绝所有写 → harness fix 直接坏掉。

这代表目前沙箱其实并没有给模型很强的限制，需要在提示词层面提示模型，不能沿着错误的方向进行各种错误的尝试。

### 12. 其他的一些反思

1. 我发现，一些 agent 工程细节，还是需要由人类把关的，有一些很工程的东西不能完全交给 AI
2. 此外还有一些问题，就是部署的问题，这些部署不能完全信任 AI，最后应当由人类来测试部署情况
3. 在项目进行过程中，我发现 superpowers 可能它的用处只体现在了约束模型，而没有达到很强的结合程序员的目的，也就是和人类交互的太少了，很多交互都是带有强烈的模型主观性的，说到底，人类好像还是处于和使用普通 agent 那样一直点击确认的情况，这是不好的，所以如何达到一个平衡点是我们需要考虑的，我也在想能否在它的基础上改进一下。
4. 我认为 agent 应当花更多时间去研究它的底层细节，而不是 vibe coding ，固然可以使用一些 AI 来达到加速完成项目的，但是对于个人提升，我认为如果能够有一个 skill 或者 md 约束模型不能够进行代码补全，只能提供知识和方向，我认为那样对我们的提升才是最大的。
5. 此外，当前coding-agent处理会很偏向简单的处理方式，它不会深入背后细节，不会把根因解决掉，有很多时候只会引入新的bug